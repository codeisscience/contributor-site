---
title: 'User Mutation'
sidebar_label: User Mutation
sidebar_position: 1
toc_max_heading_level: 4
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

### `register(userInfo)`

Register mutation adds a new user to the database.

#### `userInfo` ⟹ RegisterInput

<a className='response' href='../otherFolder/doc4.md'>
  ⮩ Paginated Journals
</a>

<Tabs>
  <TabItem value='operation' label='Operation' default>

```graphql
mutation CreateJournal($journal: JournalInput!) {
  createJournal(journalToCreate: $journal) {
    journal {
      id
      title
      url
      issn
      domainName
      createdAt
      updatedAt
      createdBy

      policies {
        title
        firstYear
        lastYear
        policyType
        isDataAvailabilityStatementPublished
        isDataShared
        isDataPeerReviewed
        enforced
        enforcedEvidence
      }
    }

    errors {
      field
      message
    }
  }
}
```

  </TabItem>

  <TabItem value='variables' label='Variables' default>

```js
function helloWorld() {
  console.log('Hello, world!');
}
```

  </TabItem>

  <TabItem value='response' label='Response'>

```js
function helloWorld() {
  console.log('Hello, world!');
}
```

  </TabItem>
</Tabs>

### `login(userInfo)`

Login mutation will allows users to log in to the website.

#### `userInfo` ⟹ LoginInput

The user needs to enter the usernameOrEmail in one field and the password in another

<a className='response' href='../otherFolder/doc4.md'>
  ⮩ Paginated Journals
</a>

<Tabs>
  <TabItem value='operation' label='Operation' default>

```graphql
mutation CreateJournal($journal: JournalInput!) {
  createJournal(journalToCreate: $journal) {
    journal {
      id
      title
      url
      issn
      domainName
      createdAt
      updatedAt
      createdBy

      policies {
        title
        firstYear
        lastYear
        policyType
        isDataAvailabilityStatementPublished
        isDataShared
        isDataPeerReviewed
        enforced
        enforcedEvidence
      }
    }

    errors {
      field
      message
    }
  }
}
```

  </TabItem>

  <TabItem value='variables' label='Variables' default>

```js
function helloWorld() {
  console.log('Hello, world!');
}
```

  </TabItem>

  <TabItem value='response' label='Response'>

```js
function helloWorld() {
  console.log('Hello, world!');
}
```

  </TabItem>
</Tabs>

### `logout`

<a className='response' href='../otherFolder/doc4.md'>
  ⮩ Paginated Journals
</a>

### `addMockUserData(numberOfUsers)`

#### `numberOfUsers` ⟹ Int

<a className='response' href='../otherFolder/doc4.md'>
  ⮩ Paginated Journals
</a>
